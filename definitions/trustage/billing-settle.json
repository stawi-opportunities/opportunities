{
  "version": "1.0",
  "name": "opportunities.billing.settle.note",
  "description": "DOCUMENTATION ONLY — settlement is no longer a bulk Trustage cron. service-billing creates one Trustage workflow per open checkout invoice (billing.invoice.settle.{invoiceId}) when checkout opens, and POSTs /_internal/billing/invoices/{id}/settle on each fire. This static definition has no schedule so it does not run.",
  "input": {},
  "config": {},
  "timeout": "1m",
  "on_error": {
    "action": "abort"
  },
  "steps": [
    {
      "id": "noop",
      "type": "call",
      "name": "No-op placeholder (do not schedule)",
      "timeout": "30s",
      "call": {
        "action": "http.request",
        "input": {
          "url": "${BILLING_INTERNAL_URL}/_internal/billing/invoices/__none__/settle",
          "method": "POST",
          "headers": {
            "Content-Type": "application/json",
            "X-Admin-Token": "${BILLING_INTERNAL_ADMIN_TOKEN}"
          },
          "body": {}
        },
        "output_var": "unused"
      }
    }
  ],
  "schedules": []
}
