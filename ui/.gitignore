node_modules/
public/
resources/
.hugo_build.lock
pagefind/

# React app build output — regenerated by Vite from app/src/
static/app/
data/app_manifest.json

# Admin sub-app build output — regenerated by Vite from admin/src/
static/admin-app/
data/admin_manifest.json

# R2-synced content (pulled during CF Pages build)
# Keep sample files in git for local dev, but real content comes from R2
