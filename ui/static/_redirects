# Cloudflare Pages rewrites (200 = silent rewrite, 301 = redirect).
#
# Dynamic detail routes are served by a single pre-built shell page; the
# React island reads the URL and hydrates from R2 / the search API.
#
# Order matters — first match wins. Literal section roots must come before
# their splat siblings, otherwise the splat swallows the listing page.
#
# Public product is jobs-only. Non-job listing roots redirect to search;
# detail shells stay for legacy deep links / internal tooling.

/jobs/             /jobs/             200
/jobs/*            /job/              200

# Non-job kinds are not marketed — send browsers to job search.
/scholarships/     /search/           301
/scholarships/*    /search/           301
/tenders/          /search/           301
/tenders/*         /search/           301
/deals/            /search/           301
/deals/*           /search/           301
/funding/          /search/           301
/funding/*         /search/           301

/categories/       /categories/       200
/categories/*      /categories/       200
