# Cloudflare Pages catch-all rewrites.
# 200 = rewrite (URL unchanged in browser); 301 would be a redirect.
# Dynamic routes are served by a single pre-built shell page; JS reads the
# URL and hydrates from R2 or the search API.
/jobs/*         /jobs/         200
/categories/*   /categories/   200
/companies/*    /companies/    200
